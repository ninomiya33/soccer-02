import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.soccermanager',
  appName: 'Soccer Manager',
  webDir: 'dist',
  // server: {
  //   url: 'https://takuma-jr.vercel.app',
  //   cleartext: true
  // }
};

export default config;